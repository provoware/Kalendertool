Startskript benutzen
====================

So starten Sie das Tool bequem:

1. Terminal öffnen.
2. Skript ausführen:

   .. code-block:: bash

      python start_gui.py

Das Skript prüft automatisch die benötigten Programme (Abhängigkeiten) und startet die grafische Oberfläche.
